server {
  listen 80;
  server_name beta.reblogme.com;

  root /srv/static-sites/beta.reblogme.com/public;
  index index.html;

  access_log /var/log/nginx/beta.reblogme.com.access.log;
  error_log /var/log/nginx/beta.reblogme.com.error.log warn;

  location / {
    try_files $uri $uri/ /index.html;
  }
}
