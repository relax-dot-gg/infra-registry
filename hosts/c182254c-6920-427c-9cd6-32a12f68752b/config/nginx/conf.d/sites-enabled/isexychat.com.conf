server {
  listen 80;
  server_name isexychat.com;

  root /srv/static-sites/isexychat.com/public;
  index index.html;

  access_log /var/log/nginx/isexychat.com.access.log;
  error_log /var/log/nginx/isexychat.com.error.log warn;

  # Large assets kept outside git
  location /blog-images/ {
    alias /srv/static-blobs/isexychat.com/blog-images/;
  }

  location /blog/ {
    alias /srv/static-blobs/isexychat.com/blog/;
    try_files $uri $uri/ =404;
  }

  location / {
    try_files $uri $uri/ /index.html;
  }
}
