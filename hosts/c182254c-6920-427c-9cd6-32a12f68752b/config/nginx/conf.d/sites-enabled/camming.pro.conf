server {
  listen 80;
  server_name camming.pro;

  root /srv/static-sites/camming.pro/public;
  index index.html;

  access_log /var/log/nginx/camming.pro.access.log;
  error_log /var/log/nginx/camming.pro.error.log warn;

  location / {
    try_files $uri $uri/ /index.html;
  }
}
