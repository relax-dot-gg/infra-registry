server {
  listen 80;
  server_name staging.cyberstorm.dev;

  root /srv/static-sites/staging.cyberstorm.dev/public;
  index index.html;

  access_log /var/log/nginx/staging.cyberstorm.dev.access.log;
  error_log /var/log/nginx/staging.cyberstorm.dev.error.log warn;

  location / {
    try_files $uri $uri/ /index.html;
  }
}
